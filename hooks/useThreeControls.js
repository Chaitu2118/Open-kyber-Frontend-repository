import { useEffect } from 'react';

export default function useThreeControls(controlsRef, deps = []) {
  useEffect(() => {
    // Placeholder for React Three Fiber control mapping
  }, deps);
  return null;
}
